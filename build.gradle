apply plugin: 'java'
apply plugin: 'idea'

sourceCompatibility = 1.8
version = '1.0'
jar {
    manifest {
        attributes 'Implementation-Title': 'WorldOfMoreWires',
                'Implementation-Version': version
    }
}

dependencies {

}


uploadArchives {
    repositories {
        flatDir {
            dirs 'bin'
        }
    }
}